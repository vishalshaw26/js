"use strict"; // treat all JS code as newer version

 console.log(3
            +
            3) //this code will work as ecpected but we need to prioritize clarity and readability

let name = "vishal"
let age = 20
let isLoggedIn = false

/* TYPES of  DataType
number = 2^53
bigint = for larger numbers
string = "assembly of characteres"
boolean = true/false
null = stand alone value (type=object) or representation of empty value
undefined = place hold for a value which is not defined
symbol =  uniquness
*/

//OBJECT
console.table([typeof name , age ,isLoggedIn   ]);


